name: CI/CD for Teams Service

on:
  push:
    branches: [ "main" ]
    paths:
      - "teams/**"
      - ".github/workflows/teams-ci.yml"
      - "nomad/teams.hcl"
      - "docker/Dockerfile.stage"

env:
  IMAGE_TAG: ${{ github.ref_name }}-${{ github.sha }}
  DOCKER_IMAGE: friendy21/teams-service
  SERVICE_NAME: teams-service
  SERVICE_PORT: 5005

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./teams
        file: ./docker/Dockerfile.stage
        push: true
        tags: |
          ${{ env.DOCKER_IMAGE }}:latest
          ${{ env.DOCKER_IMAGE }}:${{ env.IMAGE_TAG }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Nomad
      run: |
        # Clean up any existing nomad files/directories
        sudo rm -rf /usr/local/bin/nomad
        sudo rm -rf nomad
        rm -f nomad.zip LICENSE.txt
        
        # Download Nomad
        curl -fsSL -o nomad.zip https://releases.hashicorp.com/nomad/1.8.3/nomad_1.8.3_linux_amd64.zip
        
        # Extract with overwrite flag
        unzip -o nomad.zip
        
        # Move the binary to /usr/local/bin
        chmod +x nomad
        sudo mv nomad /usr/local/bin/nomad
        
        # Clean up
        rm -f nomad.zip LICENSE.txt
        
        # Verify installation
        nomad version

    - name: Deploy to Nomad
      env:
        NOMAD_ADDR: ${{ secrets.NOMAD_ADDR }}
        NOMAD_TOKEN: ${{ secrets.NOMAD_TOKEN }}
      run: |
        sed -i "s|IMAGE_TAG_PLACEHOLDER|${{ env.IMAGE_TAG }}|g" nomad/teams.hcl
        sed -i "s|DOCKER_IMAGE_PLACEHOLDER|${{ env.DOCKER_IMAGE }}|g" nomad/teams.hcl
        
        nomad job validate nomad/teams.hcl
        nomad job run nomad/teams.hcl
        
        # Wait for deployment
        MAX_ATTEMPTS=30
        ATTEMPT=0
        
        while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
          if nomad job status teams-service 2>/dev/null | grep -q "Status.*running"; then
            echo "✅ Teams service is running!"
            break
          fi
          sleep 10
          ATTEMPT=$((ATTEMPT + 1))
        done
        
        if [ $ATTEMPT -eq $MAX_ATTEMPTS ]; then
          echo "❌ Deployment timed out"
          exit 1
        fi
        
        echo "✅ Teams service deployed!"
